--- launch/nav2_navigation.launch.py
+++ launch/nav2_navigation.launch.py
@@
     messages.append(
         IncludeLaunchDescription(
             PythonLaunchDescriptionSource(localization_launch),
             launch_arguments={
                 "localization_param_dir": effective_localization_params,
                 "use_sim_time": LaunchConfiguration("use_sim_time").perform(context),
                 "enable_map_odom_tf": LaunchConfiguration("localizer_enable_map_odom_tf").perform(context),
                 "global_frame_id": global_frame_id,
                 "odom_frame_id": odom_frame_id,
                 "base_frame_id": base_frame_id,
                 "cloud_topic": cloud_topic,
                 "twist_topic": twist_topic,
                 "imu_topic": imu_topic,
+                "use_imu_preintegration": LaunchConfiguration("use_imu_preintegration").perform(context),
+                "imu_preintegration_use_base_frame_transform":
+                    LaunchConfiguration("imu_preintegration_use_base_frame_transform").perform(context),
                 "publish_lidar_tf": publish_lidar_tf,
                 "lidar_frame_id": lidar_frame_id,
@@
             DeclareLaunchArgument("imu_topic", default_value="/imu/data"),
+            DeclareLaunchArgument("use_imu_preintegration", default_value="false"),
+            DeclareLaunchArgument(
+                "imu_preintegration_use_base_frame_transform",
+                default_value="false"),
             DeclareLaunchArgument("publish_lidar_tf", default_value="true"),
